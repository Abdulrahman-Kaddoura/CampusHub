server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets directly; fall back to index.html for all other
    # paths so React Router can handle client-side navigation on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
